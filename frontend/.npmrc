# @react-three/fiber declares the React Native (Expo) renderer as an optional
# peer. npm still tries to resolve that branch and deadlocks against React 19,
# even though this is a web-only project that never loads it. Legacy peer
# resolution skips it; the installed tree is correct and complete.
legacy-peer-deps=true
