# Backend origin. In development Next proxies /api to this, so the browser
# stays single-origin and the WebSocket does not trip CORS.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000/ws
